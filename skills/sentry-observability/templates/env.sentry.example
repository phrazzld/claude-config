# Sentry Environment Variables
# Copy relevant variables to your .env.local

# =============================================================================
# REQUIRED - Core Sentry Configuration
# =============================================================================

# Data Source Name - identifies your Sentry project
# Get from: Sentry Dashboard > Project Settings > Client Keys (DSN)
NEXT_PUBLIC_SENTRY_DSN=https://examplePublicKey@o0.ingest.sentry.io/0
SENTRY_DSN=https://examplePublicKey@o0.ingest.sentry.io/0

# =============================================================================
# VERCEL INTEGRATION (Recommended)
# =============================================================================
# These are auto-set when you install the Vercel Integration:
# https://vercel.com/integrations/sentry

# Auth token for source map upload (auto-set by Vercel Integration)
# SENTRY_AUTH_TOKEN=sntrys_...

# Organization slug
# SENTRY_ORG=your-org-slug

# Project slug
# SENTRY_PROJECT=your-project-slug

# =============================================================================
# OPTIONAL - Performance & Replay Tuning
# =============================================================================

# Performance trace sampling rate (0.0 to 1.0)
# Default: 0.1 (10% of requests)
# SENTRY_TRACES_SAMPLE_RATE=0.1
# NEXT_PUBLIC_SENTRY_TRACES_SAMPLE_RATE=0.1

# Session Replay - routine session recording (0.0 to 1.0)
# Default: 0 (disabled for cost savings)
# NEXT_PUBLIC_SENTRY_REPLAYS_SESSION_SAMPLE_RATE=0

# Session Replay - error session recording (0.0 to 1.0)
# Default: 1.0 (always record when errors occur)
# NEXT_PUBLIC_SENTRY_REPLAYS_ON_ERROR_SAMPLE_RATE=1.0

# =============================================================================
# OPTIONAL - Environment Override
# =============================================================================

# Override auto-detected environment
# Usually not needed if using Vercel (auto-detects production/preview/development)
# SENTRY_ENVIRONMENT=production

# =============================================================================
# CLI OPERATIONS (for scripts/alerts)
# =============================================================================

# API token for CLI operations (create alerts, query issues, etc.)
# Different from SENTRY_AUTH_TOKEN which is for source map upload
# Get from: https://sentry.io/settings/account/api/auth-tokens/
# Scopes needed: project:read, project:write, alerts:write
# SENTRY_API_TOKEN=sntrys_...

# =============================================================================
# BACKEND (Convex, API routes, etc.)
# =============================================================================
# For backend services, only SENTRY_DSN is typically needed
# Example for Convex: npx convex env set SENTRY_DSN "https://..." --prod
