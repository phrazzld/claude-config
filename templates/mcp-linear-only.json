{
  "mcpServers": {
    "linear": {
      "command": "npx",
      "args": ["-y", "mcp-remote", "https://mcp.linear.app/sse"],
      "_description": "Linear MCP server - manage issues, projects, and comments"
    }
  }
}