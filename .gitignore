/local
/projects
/todos
/statsig
/*.bak*
*.backup
/thinktank_*/
*.log
*.jsonl
.update.lock
TODO.md
settings.json
settings.local.json
shell-snapshots/
file-history/

# Caches (regenerated at runtime)
paste-cache/
stats-cache.json
cache/
__pycache__/
*.pyc

# Session-specific task state
tasks/

# Plugin caches and machine-specific install tracking
plugins/cache/
plugins/install-counts-cache.json
plugins/installed_plugins.json
plugins/known_marketplaces.json
plugins/blocklist.json

# Chrome native host (machine-specific paths, auto-generated)
chrome/chrome-native-host

# Memory and lessons archives
agents/memory/
lessons/

# Debug logs (not skills/debug/)
/debug/

# Session plans (temporary artifacts)
plans/

# External plugin repositories (managed separately)
plugins/marketplaces/claude-code-plugins/

# Skill dependencies (generated)
skills/*/node_modules/
skills/*/pnpm-lock.yaml

# MCP server virtual environments
mcps/*/.venv/

# MCP auth cache (machine-specific)
mcp-needs-auth-cache.json

image-cache/
telemetry/
__pycache__/
teams/

# Evolve runtime data (per-project, not portable)
skills/evolve/.design-evolution/
design-memory.db
design-memory.db-shm
design-memory.db-wal

# Runtime caches (provider-specific)
.gemini/
.kimi/

# Ephemeral backups
backups/
skills/.bak/

# Shell scripts (machine-specific)
scripts/

# Temporary work directories and external project checkouts
tmp/
tmp-*/
work/
caesar-in-a-year-work/
cerberus-cloud/
moneta_link
